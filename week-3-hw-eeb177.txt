1.11.2 Hormone levels in baboons - Gesquiere, et al. 2011
	1. individual 3 recorded 61 times, individual 27 recorded 5 times.
	2. 
	3. 

1.11.3


Question 3


Qustion 4
1.mass = 47.5
print(mass)
age = 122
print(age)
print('mass', 2*mass)
print('age', age-20)
2. Hopper Grace
3. a). value of element [:4] = oxyg ; element [4:] = en ; element [:] = oxygen
b). [element -1] = n ; element [-2] = e
c). element [1:-1] = creates a substring from index 1 up to the final index while removing the first and last letter from the word "oxygen"
4. [] []
5. a). all of our plots stop just short of the upper end of the graph becuase matplotlib normally sets x and y axis to min and max of the data depending on the range
b). axes3.set_ylabel('min')
axes3.plot(numpy.min(data, axis=0))
axes3.set_ylim(0,6)
6. Because matplotlib interpolates (draws a straight line) between the points. Use the matplotlib "drawstyle" option
7. max_plot = matplotlib.pyplot.plot(numpy.std(data, axis =0))
matplotlib.pyploy.show()
8. import numpy
import matplotlib.pyplot
data = numpy.loadtxt(fname='data/inflammation-01.csv', delimiter=',')
fig = matplotlib.pyploy.figure(figsize=(3.0, 10.0))
axes1 = fig.add_subplot(3, 1, 1)
axes2 = fig.add_subplot (3, 1, 2)
axes3 = fig.add_subplot(3, 1, 3)
axes1.set_ylabel('average')
axes1.plot(numpy.mean(data,axis=0))
axes2.plot(numpy.mean(data, axis=0))
axes3.ploy(numpy.max(data, axis=0))
fig.tight_layout()
matplotlib.pyplot.show()
9. D = numpy.hstack((A[:, :1], A[:,n-1:]))
print('D = ')
print(D)
